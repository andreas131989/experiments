version: '3'
services:
  ansible:
    build: .
    image: 'ansible-adop'
    command: ["bash", "-c", "ansible-galaxy install geerlingguy.repo-epel atosatto.docker-swarm && env && ansible-playbook -c paramiko -i host quickstart.yml -v"]
    volumes:
      - .:/ansible/