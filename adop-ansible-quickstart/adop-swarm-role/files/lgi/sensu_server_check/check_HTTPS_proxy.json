{
  "checks": {
    "check_HTTPS_proxy": {
      "command": "${PLUGINS_DIR}/basic/check-http.rb -k-u https://proxy --response-code 401",
      "handlers": [
        "mailer"
      ],
      "subscribers": [
        "proxy"
      ],
      "interval": 60
    }
  }
}
