{
  "checks": {
    "check_HTTP_restheart": {
      "command": "${PLUGINS_DIR}/basic/check-http.rb -u http://restheart/_metrics -P 8080",
      "handlers": [
        "mailer"
      ],
      "subscribers": [
        "restheart"
      ],
      "interval": 60
    }
  }
}
